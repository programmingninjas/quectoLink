# I modified this compose to work with Jenkins for testing kindly update for local use
services:
  backend:
    image: programmingninjas/quectolink-backend:${BACKEND_VERSION}
    environment:
      - MONGO_URI=${MONGO_URI}
      - PORT=${PORT}
      - JWT_SECRET=${JWT_SECRET}
    ports:
      - "${PORT}:${PORT}"
  frontend:
    image: programmingninjas/quectolink:${FRONTEND_VERSION}
    environment:
      - IP=${API}
    ports:
      - "80:80"
  cache:
    image: redis:alpine
    ports:
      - "6379:6379"
  zookeeper:
    image: zookeeper
    ports:
      - "2181:2181"
  mongo:
    image: mongo
    ports:
      - "27017:27017"
