apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    name: backend-svc
    app: quectolink
spec:
  type: NodePort
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30004
  selector:
    name: backend-pod
    app: quectolink